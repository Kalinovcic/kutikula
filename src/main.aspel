package kutikula;

import "math.aspel";
import "util.aspel";
import "render.aspel";
import "logic.aspel";

main: (args: [][]uint8)
{
    open_video_system();
    while !window_should_close
    {
        handle_events();
        update();
        render();
    }
    close_video_system();
}
